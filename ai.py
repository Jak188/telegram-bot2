import os
import requests

# Example using OpenAI (or any provider)
OPENAI_KEY = os.environ.get("OPENAI_API_KEY")

def ask_ai(prompt, user_id=None):
    # Tiny stub — replace with proper call (OpenAI/Gemini)
    # For now return echo + note
    return f"[AI reply] {prompt}"
